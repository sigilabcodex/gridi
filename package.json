{
  "name": "gridi",
  "private": true,
  "version": "0.32.4",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:gh": "vite build --mode gh",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --test --watch tests/*.test.mjs",
    "test:step": "node --test tests/eventWindowAndDedupe.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "7.2.4"
  }
}
